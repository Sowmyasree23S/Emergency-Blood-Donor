services:
  - type: web
    name: blood-donor-finder
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: FLASK_DEBUG
        value: false
